<div class="block p-6 rounded-lg shadow-lg bg-white lg:w-2/3 mx-auto mt-10">
  <div class="text-center mb-5">
    <h1 class="text-xl font-bold">アカウントを新規作成</h1>
    <span class="block">or</span>
    <%= link_to 'アカウントにサインイン', new_user_session_path, class: 'text-indigo-500 hover:underline' %>
  </div>
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {novalidate: true}) do |new_user| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="form-group mb-6">
    <%= new_user.label :name, '名前', class: 'form-label inline-block mb-2 text-gray-700' %>
    <%= new_user.text_field :name, class: 'form-control
        block
        w-full
        px-3
        py-1.5
        text-base
        font-normal
        text-gray-700
        bg-white bg-clip-padding
        border border-solid border-gray-300
        rounded
        transition
        ease-in-out
        m-0
        focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none', placeholder: '名前を入力してください' %>
  </div>
  <div class="form-group mb-6">
    <%= new_user.label :email, 'メールアドレス', class: 'form-label inline-block mb-2 text-gray-700' %>
    <%= new_user.email_field :email, class: 'form-control
        block
        w-full
        px-3
        py-1.5
        text-base
        font-normal
        text-gray-700
        bg-white bg-clip-padding
        border border-solid border-gray-300
        rounded
        transition
        ease-in-out
        m-0
        focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none', placeholder: 'Emailを入力してください' %>
  </div>
  <div class="form-group mb-6">
    <%= new_user.label :password, 'パスワード', class: 'form-label inline-block mb-2 text-gray-700' %>
    <%= new_user.password_field :password, class: 'form-control
        block
        w-full
        px-3
        py-1.5
        text-base
        font-normal
        text-gray-700
        bg-white bg-clip-padding
        border border-solid border-gray-300
        rounded
        transition
        ease-in-out
        m-0
        focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none', placeholder: 'パスワードを入力してください' %>
  </div>
  <div class="form-group mb-6">
    <%= new_user.label :password, 'パスワード確認', class: 'form-label inline-block mb-2 text-gray-700' %>
    <%= new_user.password_field :password_confirmation, class: 'form-control
        block
        w-full
        px-3
        py-1.5
        text-base
        font-normal
        text-gray-700
        bg-white bg-clip-padding
        border border-solid border-gray-300
        rounded
        transition
        ease-in-out
        m-0
        focus:text-gray-700 focus:bg-white focus:border-blue-600 focus:outline-none', placeholder: 'パスワードを再度入力してください' %>
  </div>

  <%= new_user.submit 'アカウントを作成', class: 'px-6
      py-2.5
      bg-blue-600
      text-white
      font-medium
      text-md
      leading-tight
      uppercase
      rounded
      shadow-md
      hover:bg-blue-700 hover:shadow-lg
      focus:bg-blue-700 focus:shadow-lg focus:outline-none focus:ring-0
      active:bg-blue-800 active:shadow-lg
      transition
      duration-150
      ease-in-out cursor-pointer' %>
  <% end %>
</div>
